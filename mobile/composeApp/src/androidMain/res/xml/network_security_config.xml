<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        Domyslnie (base-config) wymuszamy HTTPS dla calego ruchu -
        to jest bezpieczne zachowanie produkcyjne, ktore chcemy
        zachowac dla prawdziwego backendu (np. api.taxicompany.com).
    -->
    <base-config cleartextTrafficPermitted="false" />

    <!--
        Wyjatek TYLKO dla adresow uzywanych lokalnie w trybie dev:
        - 10.0.2.2 to specjalny alias Android Emulatora na hosta
          (patrz PlatformConfig.android.kt)
        - localhost/127.0.0.1 przydatne przy testach na niektorych
          konfiguracjach (np. emulator z mapowaniem portow adb reverse)
        Bez tego wyjatku CALA aplikacja musialaby pozwalac na HTTP
        wszedzie, co byloby dziura bezpieczenstwa w wersji produkcyjnej.
    -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">192.168.1.35</domain>
    </domain-config>
</network-security-config>
